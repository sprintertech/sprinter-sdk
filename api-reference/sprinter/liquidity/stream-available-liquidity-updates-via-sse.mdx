---
title: "Stream Available Liquidity Updates via SSE"
sidebarTitle: "Stream Liquidity Updates"
openapi: get /liquidity/protocol/{protocol}/events
---

<RequestExample>
```bash cURL
curl --request GET \
  --url 'https://api.sprinter.tech/liquidity/protocol/{protocol}/events'
```

```python Python
import requests

response = requests.get("https://api.sprinter.tech/liquidity/protocol/{protocol}/events")
print(response.json())
```

```javascript JavaScript
const response = await fetch(
  "https://api.sprinter.tech/liquidity/protocol/{protocol}/events"
);
const data = await response.json();
console.log(data);
```

```go Go
package main

import (
	"fmt"
	"io"
	"net/http"
)

func main() {
	resp, _ := http.Get("https://api.sprinter.tech/liquidity/protocol/{protocol}/events")
	defer resp.Body.Close()
	body, _ := io.ReadAll(resp.Body)
	fmt.Println(string(body))
}
```
</RequestExample>