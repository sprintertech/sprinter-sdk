---
title: "Stash V1 API - Get Borrow Quote"
sidebarTitle: "Get Borrow Quote"
openapi: get /liquidity/chain/{chain}/protocol/{protocol}/type/{type}/quote
---

<RequestExample>
```bash cURL
curl --request GET \
  --url 'https://api.sprinter.tech/liquidity/chain/{chain}/protocol/{protocol}/type/{type}/quote'
```

```python Python
import requests

response = requests.get("https://api.sprinter.tech/liquidity/chain/{chain}/protocol/{protocol}/type/{type}/quote")
print(response.json())
```

```javascript JavaScript
const response = await fetch(
  "https://api.sprinter.tech/liquidity/chain/{chain}/protocol/{protocol}/type/{type}/quote"
);
const data = await response.json();
console.log(data);
```

```go Go
package main

import (
	"fmt"
	"io"
	"net/http"
)

func main() {
	resp, _ := http.Get("https://api.sprinter.tech/liquidity/chain/{chain}/protocol/{protocol}/type/{type}/quote")
	defer resp.Body.Close()
	body, _ := io.ReadAll(resp.Body)
	fmt.Println(string(body))
}
```
</RequestExample>